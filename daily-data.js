/**
 * 投资观点页签 - 待重构
 * 临时占位，不报错即可
 */

function renderSummaryContent() {
    const el = document.getElementById('summaryContent');
    if (!el) return;
    el.innerHTML = '<div style="text-align:center;padding:40px 0;color:var(--text2);font-size:14px;">⏳ 加载中...</div>';
}
